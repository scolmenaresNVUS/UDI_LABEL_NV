[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "npm ci",
  "cd client && npm ci --include=dev",
  "cd server && npm ci --include=dev"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
